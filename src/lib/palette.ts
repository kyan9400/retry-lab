import type { StrategyId } from '../types'

export const strategyColors: Record<StrategyId, string> = {
  fixed: '#ff5a2d',
  exponential: '#1239c7',
  'full-jitter': '#15856f',
  decorrelated: '#8b3f9e',
}
